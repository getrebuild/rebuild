{
  "name": "rebuild-compiler",
  "version": "0.3.0",
  "description": "rebuild js/css/html compiler (esbuild)",
  "author": "rebuild",
  "license": "GPL-3.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/getrebuild/rebuild.git"
  },
  "dependencies": {
    "esbuild": "^0.23.0"
  },
  "scripts": {
    "build": "node build.mjs"
  }
}
