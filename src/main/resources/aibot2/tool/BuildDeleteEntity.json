{
  "type": "function",
  "userDescription": "强制删除实体，包含所有数据、字段和关联配置",
  "function": {
    "name": "BuildDeleteEntity",
    "description": "强制删除实体（仅管理员）。将永久删除该实体及其所有数据、字段、关联配置（如过滤器、布局、触发器、审批流程等），若存在明细实体也会一并删除。此操作不可恢复，删除前务必先用 ListEntities 确认目标实体，并征求用户明确确认。系统内置实体不允许删除",
    "parameters": {
      "type": "object",
      "properties": {
        "entity": {
          "type": "string",
          "description": "要删除的实体标识，支持实体名称（如 Account）或实体标签（如 客户）。可通过 ListEntities 获取"
        },
        "confirmed": {
          "type": "boolean",
          "description": "是否已获得用户明确确认。首次调用不带此参数或设为 false，工具将返回改动清单供你转述给用户；用户明确同意后再以相同参数并设置 confirmed=true 重新调用执行删除"
        }
      },
      "required": ["entity"],
      "additionalProperties": false
    }
  }
}
