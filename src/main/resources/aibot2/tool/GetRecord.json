{
  "type": "function",
  "userDescription": "根据记录 ID 获取单条记录的完整字段详情",
  "function": {
    "name": "GetRecord",
    "description": "获取单条记录的完整字段详情，通过记录ID查询，返回所有可查询字段的值。当用户想查看某条记录的完整信息（如「看下这条记录的详情」）时使用。需要查询多条记录或按条件筛选请用 QueryRecords",
    "parameters": {
      "type": "object",
      "properties": {
        "entity": {
          "type": "string",
          "description": "实体名称、数字code或标签名称（可通过 ListEntities 工具查询可用实体）"
        },
        "recordId": {
          "type": "string",
          "description": "记录ID（可通过 QueryRecords 工具查询获取）"
        },
        "fields": {
          "type": "string",
          "description": "返回的字段名称列表（逗号分隔），不传则返回所有可查询字段"
        }
      },
      "required": ["entity", "recordId"],
      "additionalProperties": false
    }
  }
}
