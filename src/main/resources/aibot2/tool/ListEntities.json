{
  "type": "function",
  "function": {
    "name": "ListEntities",
    "description": "查看实体及其字段结构。不传名称返回所有可用实体列表（当前用户是管理员时，列表还会包含用户、部门、角色、团队等组织架构实体），传入名称返回该实体的完整字段定义。在进行记录查询、统计、导出等操作前建议先调用此工具获取实体名称和字段信息",
    "parameters": {
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "description": "实体名称、数字code或标签名称，支持模糊匹配（可选，不传则返回所有实体列表）"
        }
      },
      "additionalProperties": false
    }
  }
}
