{
  "type": "function",
  "function": {
    "name": "UpsertRecord",
    "description": "解析文件或文本内容为业务记录并保存，支持 PDF、Word、Excel 等格式。传入 recordId 时更新已有记录，不传时新建记录。支持同时保存明细实体数据（主从结构）。设置 outputJson=true 可仅解析预览不保存",
    "parameters": {
      "type": "object",
      "properties": {
        "file": {
          "type": "string",
          "description": "文件路径或标识（支持系统内标识、本地路径、外部URL）。与 content 二选一"
        },
        "content": {
          "type": "string",
          "description": "文本内容（直接传入，无需文件路径）。与 file 二选一"
        },
        "entity": {
          "type": "string",
          "description": "目标实体名称或标识（可通过 ListEntities 工具查询）"
        },
        "recordId": {
          "type": "string",
          "description": "要更新的记录ID（可选，不传则新建）。可通过 QueryRecords 工具查询获取"
        },
        "outputJson": {
          "type": "boolean",
          "description": "是否仅输出解析后的记录JSON而不保存，默认false。设为true则只返回解析的JSON数据，不保存记录"
        }
      },
      "required": ["entity"]
    }
  }
}
