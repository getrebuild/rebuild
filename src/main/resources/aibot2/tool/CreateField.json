{
  "type": "function",
  "function": {
    "name": "CreateField",
    "description": "新建字段（需管理员权限）。不同类型的字段需要提供不同的信息，引用字段需说明引用哪个实体，下拉列表/多选字段需提供选项，分类字段需说明使用哪个分类数据",
    "parameters": {
      "type": "object",
      "properties": {
        "entity": {
          "type": "string",
          "description": "要添加字段的实体，如 客户"
        },
        "fieldLabel": {
          "type": "string",
          "description": "字段名称，如 联系电话、订单金额"
        },
        "type": {
          "type": "string",
          "description": "字段类型，可选值包括 文本(TEXT), 多行文本(NTEXT), 整数(NUMBER), 小数(DECIMAL), 日期(DATE), 日期时间(DATETIME), 时间(TIME), 布尔(BOOL), 邮箱(EMAIL), 链接(URL), 电话(PHONE), 位置(LOCATION), 标签(TAG), 自动编号(SERIES), 图片(IMAGE), 附件(FILE), 下拉列表(PICKLIST), 多选(MULTISELECT), 分类(CLASSIFICATION), 引用(REFERENCE), 多引用(N2NREFERENCE)",
          "enum": ["TEXT", "NTEXT", "NUMBER", "DECIMAL", "DATE", "DATETIME", "TIME", "BOOL", "EMAIL", "URL", "PHONE", "LOCATION", "TAG", "SERIES", "IMAGE", "FILE", "PICKLIST", "MULTISELECT", "CLASSIFICATION", "REFERENCE", "N2NREFERENCE"]
        },
        "refEntity": {
          "type": "string",
          "description": "要引用的实体（引用字段必填），如 客户"
        },
        "options": {
          "type": "array",
          "items": { "type": "string" },
          "description": "下拉选项，第一个为默认值（下拉列表/多选字段必填），如 [\"选项1\", \"选项2\"]"
        },
        "classification": {
          "type": "string",
          "description": "要使用的分类数据（分类字段必填）"
        },
        "seriesFormat": {
          "type": "string",
          "description": "编号规则（自动编号字段可选），如 {YYYYMMDD}-{0000}"
        },
        "comments": {
          "type": "string",
          "description": "字段描述"
        }
      },
      "required": ["entity", "fieldLabel", "type"]
    }
  }
}
