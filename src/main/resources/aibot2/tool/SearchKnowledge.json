{
  "type": "function",
  "function": {
    "name": "SearchKnowledge",
    "description": "搜索系统知识库，获取与查询相关的知识片段",
    "parameters": {
      "type": "object",
      "properties": {
        "query": {
          "type": "string",
          "description": "搜索查询语句，支持自然语言提问，系统会从知识库中匹配最相关的知识片段"
        },
        "topK": {
          "type": "integer",
          "description": "返回结果数量，默认5，最大20"
        }
      },
      "required": ["query"]
    }
  }
}
