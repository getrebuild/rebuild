{
  "type": "function",
  "userDescription": "请求外部 HTTP 接口获取数据或触发操作，支持 GET/POST 方法及自定义请求头",
  "function": {
    "name": "FetchUrl",
    "description": "请求外部 HTTP 接口，支持 GET/POST 方法及自定义请求头。当用户需要调用第三方/外部 API 获取数据或触发操作时使用，会对请求 URL 进行安全检查，自动过滤 host、connection、authorization 等不安全请求头，响应内容超过 20000 字符会自动截断",
    "parameters": {
      "type": "object",
      "properties": {
        "url": {
          "type": "string",
          "description": "请求的 URL 地址（必须包含 http:// 或 https://）"
        },
        "method": {
          "type": "string",
          "description": "HTTP 请求方法，支持 GET 或 POST，默认 GET",
          "enum": ["GET", "POST"]
        },
        "headers": {
          "type": "object",
          "description": "HTTP 请求头，键值对形式。系统会自动过滤 host、connection、authorization 等不安全请求头",
          "properties": {}
        },
        "body": {
          "type": "string",
          "description": "POST 请求体内容，支持 JSON 字符串或纯文本（仅 POST 方法时使用）"
        }
      },
      "required": ["url"],
      "additionalProperties": false
    }
  }
}
